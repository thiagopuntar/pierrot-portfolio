import styled from "styled-components";

const SectionTitle = styled.h2`
  font-size: 2rem;
  font-weight: 600;
  text-align: center;
  color: var(--primary-2);
  margin-bottom: 4rem;
`;

export default SectionTitle;
